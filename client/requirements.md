## Packages
(none needed)

## Notes
- Ensure the backend includes realistic seed data for `/api/sinais`, `/api/alertas`, and `/api/relatorio` so the dashboard has rich content immediately.
- The UI uses dark mode natively and leverages the existing `lucide-react` for beautiful casino-styled iconography.
- Forms use `@hookform/resolvers/zod` with `react-hook-form` to validate against `@shared/schema`.
